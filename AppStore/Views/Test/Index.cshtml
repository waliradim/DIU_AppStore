@model IEnumerable<AppStore.Models.Tbl_AssignCourse>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LoginRegLayoutPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tbl_Course.CourseCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tbl_Level.LLevel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tbl_Semester.SemesterName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tbl_Teacher.Tname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_Course.CourseCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_Level.LLevel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_Semester.SemesterName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_Teacher.Tname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
